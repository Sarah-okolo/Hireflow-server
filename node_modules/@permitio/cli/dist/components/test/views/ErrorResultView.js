import React from 'react';
import { Text } from 'ink';
const ErrorResultView = ({ result }) => (React.createElement(React.Fragment, null,
    React.createElement(Text, null,
        "User: ",
        result.auditLog.user_key || result.auditLog.user_id),
    React.createElement(Text, null,
        "Resource: ",
        result.auditLog.resource,
        " (type:",
        ' ',
        result.auditLog.resource_type,
        ")"),
    React.createElement(Text, null,
        "Action: ",
        result.auditLog.action),
    React.createElement(Text, null,
        "Tenant: ",
        result.auditLog.tenant),
    React.createElement(Text, { color: "yellow" },
        "Error: ",
        result.error)));
export default ErrorResultView;
