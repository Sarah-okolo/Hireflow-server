import React from 'react';
import { Box, Text } from 'ink';
import ErrorResultView from './ErrorResultView.js';
import DifferenceResultView from './DifferenceResultView.js';
const DifferencesView = ({ results }) => (React.createElement(Box, { flexDirection: "column", marginBottom: 1 },
    React.createElement(Text, { bold: true, underline: true }, "Differences found:"),
    results.map((result, i) => (React.createElement(Box, { key: i, flexDirection: "column", marginTop: 1, paddingLeft: 2 }, result.error ? (React.createElement(ErrorResultView, { result: result })) : (React.createElement(DifferenceResultView, { result: result })))))));
export default DifferencesView;
