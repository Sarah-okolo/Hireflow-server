import React from 'react';
export type GeneratePolicySnapshotProps = {
    dryRun: boolean;
    models: string[];
    path?: string;
    isTestTenant?: boolean;
    snippet?: 'jest' | 'pytest' | 'vitest';
    snippetPath?: string;
};
export declare function GeneratePolicySnapshot({ dryRun, models, path, snippet, }: GeneratePolicySnapshotProps): React.JSX.Element;
