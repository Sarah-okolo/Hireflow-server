import React from 'react';
type Props = {
    onComplete: () => void;
    onError: (error: string) => void;
};
export default function EnforceComponent({ onComplete, onError }: Props): React.JSX.Element | null;
export {};
