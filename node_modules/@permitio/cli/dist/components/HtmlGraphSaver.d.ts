interface GraphNodeData {
    id: string;
    label: string;
}
interface GraphNode {
    data: GraphNodeData;
    classes?: string;
}
interface GraphEdgeData {
    source: string;
    target: string;
    label: string;
}
interface GraphEdge {
    data: GraphEdgeData;
    classes: string;
}
interface GraphData {
    nodes: GraphNode[];
    edges: GraphEdge[];
}
export declare const saveHTMLGraph: (graphData: GraphData) => void;
export {};
