import React from 'react';
type Props = {
    environment?: string;
    project?: string;
    email?: string;
    role?: 'admin' | 'write' | 'read';
    inviter_name?: string;
    inviter_email?: string;
};
export default function MemberComponent({ environment, project, email: emailP, role: roleP, inviter_email, inviter_name, }: Props): React.JSX.Element;
export {};
