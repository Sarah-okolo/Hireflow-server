import React from 'react';
import { getFiles } from '../../../lib/env/template/utils.js';
import { Text, Box } from 'ink';
export default function ListComponent() {
    const files = getFiles() || [];
    return (React.createElement(Box, { paddingLeft: 2, flexDirection: "column" },
        React.createElement(Text, { bold: true, underline: true }, "Templates List"),
        files.length > 0 ? (files.map((file, index) => React.createElement(Text, { key: index },
            "\u2022 ",
            file))) : (React.createElement(Text, null, "No Templates found."))));
}
