import React from 'react';
import { Box, Text } from 'ink';
import Spinner from 'ink-spinner';
/**
 * Component for displaying processing status and results
 */
export default function OpenapiResults({ status, error, progress, processingDone, }) {
    // Loading state
    if (status === 'loading' && !processingDone) {
        return (React.createElement(Box, null,
            React.createElement(Text, null,
                React.createElement(Spinner, { type: "dots" }),
                " ",
                progress || 'Processing...')));
    }
    // Error state
    if (status === 'error') {
        return (React.createElement(Box, { flexDirection: "column" },
            React.createElement(Text, { color: "red" },
                "Error: ",
                error),
            React.createElement(Text, null, "Please try again with a valid OpenAPI spec file.")));
    }
    // Success state
    if (status === 'success') {
        return (React.createElement(Box, { flexDirection: "column" },
            React.createElement(Text, { color: "green" }, "\u2713 OpenAPI spec successfully applied!"),
            React.createElement(Text, null, "Resources, actions, roles, and URL mappings have been created based on the OpenAPI spec.")));
    }
    // Unexpected state fallback
    return React.createElement(Text, null, "Unexpected state. Please try again.");
}
