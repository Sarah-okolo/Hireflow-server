import React from 'react';
import { Box, Text } from 'ink';
import TextInput from 'ink-text-input';
/**
 * Form component for entering the OpenAPI spec file path
 */
export default function OpenapiForm({ inputPath, setInputPath, onSubmit, }) {
    return (React.createElement(Box, { flexDirection: "column" },
        React.createElement(Text, null, "Enter the path to your OpenAPI spec file:"),
        React.createElement(Box, null,
            React.createElement(TextInput, { value: inputPath, onChange: setInputPath, onSubmit: onSubmit, placeholder: "Path to local file or URL (e.g., ./openapi.json or https://example.com/api.json)" }))));
}
