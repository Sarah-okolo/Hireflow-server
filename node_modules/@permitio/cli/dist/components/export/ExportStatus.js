import React from 'react';
import { Text } from 'ink';
import Spinner from 'ink-spinner';
export const ExportStatus = ({ state, file }) => {
    if (state.error) {
        return (React.createElement(React.Fragment, null,
            React.createElement(Text, { color: "red" },
                "Error: ",
                state.error),
            state.warnings.length > 0 && (React.createElement(React.Fragment, null,
                React.createElement(Text, { color: "yellow" }, "Warnings:"),
                state.warnings.map((warning, i) => (React.createElement(Text, { key: i, color: "yellow" },
                    "- ",
                    warning)))))));
    }
    if (!state.isComplete) {
        return (React.createElement(React.Fragment, null,
            React.createElement(Text, null,
                React.createElement(Spinner, { type: "dots" }),
                ' ',
                state.status || 'Exporting environment configuration...')));
    }
    return (React.createElement(React.Fragment, null,
        React.createElement(Text, { color: "green" }, "Export completed successfully!"),
        file && React.createElement(Text, null,
            "HCL content has been saved to: ",
            file),
        state.warnings.length > 0 && (React.createElement(React.Fragment, null,
            React.createElement(Text, { color: "yellow" }, "Warnings during export:"),
            state.warnings.map((warning, i) => (React.createElement(Text, { key: i, color: "yellow" },
                "- ",
                warning)))))));
};
