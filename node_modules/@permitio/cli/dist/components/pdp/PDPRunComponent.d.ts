import React from 'react';
type Props = {
    opa?: number;
    dryRun?: boolean;
    onComplete?: () => void;
    onError?: (error: string) => void;
    skipWaitScreen?: boolean;
};
export default function PDPRunComponent({ opa, dryRun, onComplete, onError, skipWaitScreen, }: Props): React.JSX.Element;
export {};
