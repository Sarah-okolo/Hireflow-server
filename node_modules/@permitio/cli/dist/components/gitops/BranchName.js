import React from 'react';
import { Box, Text } from 'ink';
import TextInput from 'ink-text-input';
const BranchName = ({ onBranchSubmit, onError }) => {
    const [branchName, setBranchName] = React.useState('');
    const handleBranchSubmit = () => {
        if (branchName.length <= 1) {
            onError('Please enter a valid branch name');
            return;
        }
        onBranchSubmit(branchName);
    };
    return (React.createElement(React.Fragment, null,
        React.createElement(Box, { margin: 1 },
            React.createElement(Text, { color: 'green' }, " Enter the Branch Name: "),
            React.createElement(TextInput, { value: branchName, onChange: setBranchName, onSubmit: handleBranchSubmit }))));
};
export default BranchName;
