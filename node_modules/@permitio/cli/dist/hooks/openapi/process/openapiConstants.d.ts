export declare const ERROR_CREATING_RESOURCE = "Failed to create resource";
export declare const ERROR_CREATING_ROLE = "Failed to create role";
export declare const ERROR_UPDATING_ROLE = "Failed to update role";
export declare const ERROR_CREATING_RESOURCE_ROLE = "Failed to create resource role";
export declare const PERMIT_EXTENSIONS: {
    RESOURCE: string;
    ACTION: string;
    ROLE: string;
    RESOURCE_ROLE: string;
    RELATION: string;
    DERIVED_ROLE: string;
};
export interface ResourceKey {
    key: string;
}
export interface RoleKey {
    key: string;
}
export interface RoleWithPermissions {
    permissions?: string[];
}
export interface UrlMapping {
    url: string;
    http_method: string;
    resource: string;
    action: string;
}
export interface ProcessorContext {
    resources: Set<string>;
    actions: Map<string, Set<string>>;
    roles: Set<string>;
    resourceRoles: Map<string, boolean>;
    relations: Map<string, string>;
    mappings: UrlMapping[];
    errors: string[];
    warnings: string[];
    existingResources: ResourceKey[];
    existingRoles: RoleKey[];
    baseUrl: string;
}
export interface ProcessorProps {
    inputPath: string;
    setProgress: (message: string) => void;
    setStatus: (status: 'loading' | 'error' | 'success') => void;
    setError: (error: string | null) => void;
    setProcessingDone: (done: boolean) => void;
}
