import { Permit } from 'permitio';
import { HCLGenerator, WarningCollector } from '../types.js';
export declare class ConditionSetGenerator implements HCLGenerator {
    private permit;
    private warningCollector;
    name: string;
    private template;
    constructor(permit: Permit, warningCollector: WarningCollector);
    /**
     * Process an individual condition set rule and convert it to the expected format
     * Returns null if the rule should be skipped
     */
    private processConditionSetRule;
    generateHCL(): Promise<string>;
}
