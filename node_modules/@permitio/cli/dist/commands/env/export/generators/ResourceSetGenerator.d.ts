import { Permit } from 'permitio';
import { HCLGenerator, WarningCollector } from '../types.js';
export declare class ResourceSetGenerator implements HCLGenerator {
    private permit;
    private warningCollector;
    name: string;
    private template;
    private resourceKeyMap;
    private resourceAttributes;
    constructor(permit: Permit, warningCollector: WarningCollector);
    private fetchResourceAttributes;
    private detectAdditionalDependencies;
    generateHCL(): Promise<string>;
    private formatConditions;
}
