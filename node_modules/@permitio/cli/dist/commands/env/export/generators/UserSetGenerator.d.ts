import { Permit } from 'permitio';
import { HCLGenerator, WarningCollector } from '../types.js';
interface UserAttribute {
    key: string;
    resourceKey: string;
    type: string;
    description: string;
}
export declare class UserSetGenerator implements HCLGenerator {
    private permit;
    private warningCollector;
    name: string;
    private template;
    private sharedUserAttributes;
    constructor(permit: Permit, warningCollector: WarningCollector);
    setUserAttributes(attributes: UserAttribute[]): void;
    private fetchUserAttributes;
    private detectDependencies;
    private processConditions;
    generateHCL(): Promise<string>;
}
export {};
