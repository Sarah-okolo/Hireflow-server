import { Permit } from 'permitio';
import { HCLGenerator, WarningCollector } from '../types.js';
export declare class RoleDerivationGenerator implements HCLGenerator {
    private permit;
    private warningCollector;
    name: string;
    private template;
    private relationIdMap;
    private roleIdMap;
    private processedDerivations;
    constructor(permit: Permit, warningCollector: WarningCollector);
    setRelationIdMap(relationIdMap: Map<string, string>): void;
    setRoleIdMap(roleIdMap: Map<string, string>): void;
    private getRoleTerraformId;
    private findRelationTerraformName;
    private createDerivationId;
    private createDependenciesList;
    private fetchResources;
    private processRoleGrant;
    private processRoleGrants;
    private processResource;
    generateHCL(): Promise<string>;
}
