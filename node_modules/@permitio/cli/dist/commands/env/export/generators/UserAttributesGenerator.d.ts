import { Permit } from 'permitio';
import { HCLGenerator, WarningCollector } from '../types.js';
export declare class UserAttributesGenerator implements HCLGenerator {
    private permit;
    private warningCollector;
    name: string;
    private template;
    constructor(permit: Permit, warningCollector: WarningCollector);
    private loadTemplate;
    private getUserAttributes;
    private generateResourceKey;
    private normalizeAttributeType;
    generateHCL(): Promise<string>;
}
