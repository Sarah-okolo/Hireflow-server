import { WarningCollector } from './types.js';
export declare function createSafeId(...parts: string[]): string;
export declare function createWarningCollector(): WarningCollector;
export declare const generateProviderBlock: () => string;
export interface PaginatedResponse<T> {
    data: T[];
    pagination?: {
        total_count?: number;
        page?: number;
        per_page?: number;
    };
}
/**
 * Generic utility to fetch all pages of results from any list endpoint
 */
export declare function fetchList<T, P extends Record<string, unknown>>(listFunction: (params: P) => Promise<T[] | PaginatedResponse<T>>, baseParams: P, page?: number, perPage?: number): Promise<T[]>;
