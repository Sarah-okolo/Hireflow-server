import React from 'react';
import { option } from 'pastel';
import zod from 'zod';
import { AuthProvider } from '../../../components/AuthProvider.js';
import ListComponent from '../../../components/env/template/ListComponent.js';
export const description = 'A simple command which lists all the terraform templates available.';
export const options = zod.object({
    apiKey: zod
        .string()
        .optional()
        .describe(option({
        description: 'Optional: API Key to be used for the environment to apply the terraform template.',
    })),
});
export default function List({ options: { apiKey } }) {
    return (React.createElement(React.Fragment, null,
        React.createElement(AuthProvider, { permit_key: apiKey, scope: 'environment' },
            React.createElement(ListComponent, null))));
}
