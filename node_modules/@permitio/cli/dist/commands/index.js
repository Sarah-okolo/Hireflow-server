import React from 'react';
import Gradient from 'ink-gradient';
import { Text, Box, Newline } from 'ink';
import { AuthProvider } from '../components/AuthProvider.js';
import EnvironmentInfo from '../components/EnvironmentInfo.js';
export default function Index() {
    return (React.createElement(AuthProvider, { skipLogin: true },
        React.createElement(Box, { borderStyle: "single", margin: 2, flexDirection: "column" },
            '21.3.0'.localeCompare(process.versions.node, undefined, {
                numeric: true,
                sensitivity: 'base',
            }) > 0 && (React.createElement(Text, { dimColor: true },
                "\uD83D\uDE80 Permit CLI is best supported on Node.js v22+, upgrade your Node version to get the best experience of the tool: https://nodejs.org/en/download",
                React.createElement(Newline, { count: 1 }))),
            React.createElement(Text, null,
                React.createElement(Gradient, { colors: ['#FF923F', '#944EEF'] }, "Permit CLI"),
                " is a developer swiss army knife for fine-grained authorization"),
            React.createElement(Text, null, "Run this command with --help for more information"),
            React.createElement(Newline, { count: 1 }),
            React.createElement(EnvironmentInfo, null))));
}
