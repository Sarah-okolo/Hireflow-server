import React from 'react';
import { AuthProvider } from '../../../components/AuthProvider.js';
import { object, string } from 'zod';
import { option } from 'pastel';
import { TFChatComponent } from '../../../components/policy/create/TFChatComponent.js';
export const options = object({
    apiKey: string()
        .optional()
        .describe(option({
        description: 'Your Permit.io API key',
    })),
});
export default function AI({ options }) {
    return (React.createElement(AuthProvider, { scope: 'environment', permit_key: options.apiKey },
        React.createElement(TFChatComponent, null)));
}
