import React from 'react';
import { AuthProvider } from '../components/AuthProvider.js';
import { option } from 'pastel';
import zod from 'zod';
import InitWizardComponent from '../components/init/InitWizardComponent.js';
export const description = 'A wizard to take users through all the steps, from configuring policy to enforcing it.';
export const options = zod.object({
    apiKey: zod
        .string()
        .optional()
        .describe(option({ description: 'Your Permit.io API key' })),
});
export default function Init({ options }) {
    return (React.createElement(AuthProvider, { permit_key: options.apiKey, scope: 'environment' },
        React.createElement(InitWizardComponent, { options: options })));
}
